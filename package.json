{
  "name": "root",
  "private": true,
  "scripts": {
    "postinstall": "npm --prefix ./pipecat-web-ui install",
    "dev": "npm --prefix ./pipecat-web-ui run dev",
    "build": "npm --prefix ./pipecat-web-ui run build",
    "start": "npm --prefix ./pipecat-web-ui run start"
  },
  "engines": { "node": ">=18" }
}